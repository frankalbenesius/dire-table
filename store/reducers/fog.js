const defaultState = [
  [[-4, -4], [-4, 5], [6, 5], [6, -4]],
  // TODO: add support for fog holes & other multiple Fog areas
]

export default function reducer(state = defaultState, action) {
  switch (action.type) {
    default: return state
  }
}
